datetime==5.5
wandb==0.26.1  # 0.18.7 rejects new 86-char API keys (wandb_v1_... format); 0.25.1+ supports them
# tensorboard  # uncomment to enable Lightning Fabric TensorBoardLogger (-tensorboard_logging_flag)
numpy<3.0
matplotlib==3.9.4
scikit-learn==1.6.1
transformers==4.57.6  # Updated: 4.21.3 requires Rust to build tokenizers on Python 3.11
scipy==1.13.1
python-dotenv==1.2.1
# pandas
# openpyxl
# xlsxwriter
# scalene
pytorch-lightning==2.1.0  # Specify version
lightning==2.1.0  # Match pytorch-lightning version
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.1.0  # Match with Lightning version
torchvision==0.16.0
torchaudio==2.1.0
requests==2.32.5
urllib3<3.0.0
chardet==5.2.0
charset-normalizer==3.4.7  # Added this for complete compatibility
bitsandbytes==0.48.2
seaborn==0.13.2
debugpy
nvidia-ml-py==13.610.43
psutil==7.2.2
deepspeed==0.19.1  # Optional: for DeepSpeed distributed strategy. Must match PyTorch 2.1.0 (>=0.15 requires PyTorch 2.4+)
# pynvml - removed: nvidia-ml-py is the newer replacement
# --- Developer tooling (enabled by default via `make dev`; see Makefile + .pre-commit-config.yaml) ---
pre-commit==4.3.0  # manages the git hook that runs ruff/shfmt on commit
ruff==0.15.15  # pinned to match the ruff rev in .pre-commit-config.yaml (used by `make quality`/`make style`)
