{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run-train-and-forget-script",
            "type": "shell",
            "command": "bash",
            "args": [
                "-c",
                "source gpu_env/bin/activate && cd src && DEBUGGER=1 bash MAIN.sh 4,5,6,7"
                // "cd /app/host/src && DEBUGGER=1 bash MAIN.sh 0"
                // "cd /app/host/src && DEBUGGER=1 SCALENE=1 bash MAIN.sh 4,5,6,7"
            ],
            "options": {
                // "cwd": "/app/host" // Docker Dev Container
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}
